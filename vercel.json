{
  "buildCommand": "cd app && npm install && VERCEL=1 npm run build",
  "outputDirectory": "app/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
